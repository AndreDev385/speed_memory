/// Enum to define the display mode for the DisplayMatrix widget.
enum DisplayMode {
  /// Normal display mode.
  normal,

  /// Display mode for a correctly matched matrix.
  correct,

  /// Display mode for an incorrectly matched matrix.
  incorrect,
}