/// Archivo que contiene las dimensiones constantes utilizadas en toda la aplicación.
/// Esto permite centralizar los valores de espaciado y tamaños para facilitar el mantenimiento.
class AppDimensions {
  // Paddings
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 20.0;

  // Margins
  static const double marginVerticalCard = 8.0;
  static const double spacingBetweenElements = 20.0;
  static const double spacingHorizontalButtons = 8.0;

  // Tamaños fijos
  static const double buttonHeight = 60.0;
  static const double maxWidthConstraint = 600.0;
}
